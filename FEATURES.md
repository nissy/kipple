# Kipple Feature Guide

Kipple は、macOS のメニューバーから使えるテキスト向けクリップボードマネージャーです。
コピーしたテキストを自動で記録し、あとから検索、再コピー、編集、分類、順番貼り付けできます。

データは Mac の中に保存されます。クリップボード履歴、設定、カテゴリ情報は外部サービスへ送信されません。

## 主な特徴

- コピーしたテキストを自動で履歴に保存
- 履歴を検索、ピン留め、カテゴリで整理
- コピー元アプリとウィンドウ名を表示
- 履歴を選んでクリップボードへ戻す
- Live Editor で内容を整えてから保存
- JSON / YAML の整形に対応
- 複数の履歴を順番に貼り付ける Queue
- 画面上の文字を範囲選択して OCR で取り込み
- 日本語 / 英語 UI に対応

## クリップボード履歴

Kipple はテキストのコピーを監視し、履歴として保存します。
履歴は新しい順に表示され、同じ内容をもう一度コピーした場合は重複を増やさず、既存の項目を先頭へ移動します。

履歴には次の情報が含まれます。

- コピーしたテキスト
- コピー日時
- コピー元アプリ
- コピー元ウィンドウ名
- URL か通常テキストかの分類
- ピン留め状態
- ユーザーが割り当てたカテゴリ

履歴行にマウスを重ねると、内容のプレビュー、文字数、コピー日時、コピー元情報を確認できます。
長いテキストはプレビューで先頭部分を確認できます。

## 検索とフィルタ

履歴上部の検索欄から、コピーした内容をすばやく絞り込めます。
検索対象には本文とコピー元アプリ名が含まれます。

フィルタでは次の絞り込みができます。

- ピン留め項目のみ表示
- URL のみ表示
- ユーザー定義カテゴリで表示
- None カテゴリで表示

カテゴリフィルタは、カテゴリ管理画面で表示 / 非表示を切り替えられます。

## ピン留め

よく使う履歴はピン留めできます。
ピン留めした項目は履歴削除の対象から外れ、重要なテキストを残しておけます。

ピン留め数には上限があります。
初期値は 50 件で、設定から 1〜100 件の範囲で変更できます。

## カテゴリ

1件の履歴に複数のカテゴリを付けられます。
URLらしいテキストには `URL`、画面から取り込んだテキストには `OCR`、MCP経由の登録には `AI` が自動で付きます。
自動カテゴリも履歴行のカテゴリボタンから手動で付け外しでき、外した状態は再コピー後も維持されます。
手動でAIを付けても、取り込み元の記録は変わりません。

ユーザーは独自カテゴリを追加できます。
カテゴリには名前とアイコンを設定でき、履歴行のチェック式ポップオーバーから各項目へ割り当てられます。
複数のカテゴリがある行は、重なった四角の専用アイコンに件数を表示します。
詳細ではカテゴリごとにアイコンと名前を独立したラベルで表示し、横幅に合わせて折り返します。
ホバープレビューで収まらない分は「＋件数」で示し、右クリックの「項目の詳細」では全カテゴリをスクロールして確認できます。
カテゴリが1つも付いていない履歴は既存の「None（未分類）」で表示します。

絞り込みは履歴上部のフィルタボタンから行います。
複数選択時は「すべて含む」「いずれかを含む」を切り替えられ、検索・ピン留めの条件とも組み合わせられます。

カテゴリ管理では次の操作ができます。

- カテゴリの追加
- 名前の変更
- アイコンの変更
- フィルタへの表示 / 非表示
- ユーザー定義カテゴリの削除

カテゴリを削除しても、履歴本文と他のカテゴリは残ります。
組み込みカテゴリは削除できません。

## URL を開く

URL や URI として開ける履歴項目は、設定した修飾キーを押しながらクリックすると開けます。
コンテキストメニューから開くこともできます。

対応する代表例は次の通りです。

- `http`
- `https`
- `ftp`
- `mailto`
- アプリ側でハンドラが登録されている URI スキーム

通常クリックでは、項目をクリップボードへ戻します。
誤操作を防ぐため、URL を開く操作には修飾キーが必要です。

## Live Editor

Kipple には、現在のクリップボード内容を確認・編集するエディタがあります。

エディタでは次の操作ができます。

- 現在のクリップボード内容を表示
- ダブルクリックまたは編集ボタンで編集開始
- 編集内容を履歴に保存
- 内容をクリア
- 前後の空白や改行をトリム
- JSON を読みやすく整形
- YAML を読みやすく整形

エディタは行番号付きです。

編集内容を履歴に保存すると、コピー元は Kipple の Live Editor として記録されます。
同じ内容を保存した場合は、通常の履歴と同じく重複を増やさず先頭へ移動します。

## 行分割保存

履歴項目のコンテキストメニューから、複数行のテキストを行ごとに分割して履歴へ追加できます。
空行は除外され、各行は前後の空白を取り除いて保存されます。

住所、リスト、フォーム入力用の値などをまとめてコピーしたあと、個別の履歴項目として使いたい場合に便利です。

## Queue

Queue は、複数の履歴項目を指定した順番で貼り付ける機能です。
フォーム入力や定型作業のように、複数の値を順番に貼り付けたい場面で使えます。

Queue を有効にすると、履歴項目をクリックしてキューに追加できます。
追加された項目には順番の番号が表示されます。

主な操作は次の通りです。

- Queue ボタンで Queue モードを開始 / 終了
- クリックで項目を追加または解除
- Shift + クリックで範囲選択
- 貼り付け操作の直前に対象項目をクリップボードへ用意し、次の操作まで保持する
- Command + V を押すたびに次の項目へ進む
- 書式なし貼り付けのショートカット（初期値: Command + Shift + V）でも順に貼り付けられる
- 設定 → 一般 → 貼り付けで書式付き・書式なしを入れ替え可能。オンではCommand + Vが書式なし、もう一方のショートカットが書式付きになる
- Loop を有効にすると、最後まで進んだあと先頭へ戻る
- 終了後も最後に貼り付けた項目はクリップボードに残る

Queue は貼り付け先へ Command + V を送信するため、「デバイスの制御とデータへのアクセス」の許可が必要です。
権限がない場合、Queue ボタンには警告が表示されます。設定の Permission タブからシステム設定を開き、そこで許可してください。

## Paste on Selection

Paste on Selection を有効にすると、履歴項目を選んだときにクリップボードへ戻すだけでなく、前面アプリへ自動貼り付けできます。

この機能も貼り付け操作を送信するため、「デバイスの制御とデータへのアクセス」の許可が必要です。
権限がない場合は設定項目が無効になります。

## 画面テキストキャプチャ

Screen Text Capture は、画面上の文字を範囲選択して OCR で取り込む機能です。
選択した範囲の画像を一時的に取得し、macOS の Vision フレームワークでテキストを認識します。

認識したテキストは次のように扱われます。

- 段落の区切りを空行で保持
- 表はタブ区切り（TSV）でコピーし、空セルの列位置を保持
- 前後の不要な改行を取り除く
- クリップボードへコピー
- 履歴へ保存
- Kipple のメインウィンドウを開く

認識言語は日本語と英語に対応しています。
画面を読み取るため、「画面収録とシステムオーディオ録音」の許可が必要です。音声は録音しません。
取得した画像と認識テキストは端末内で処理され、外部へ送信されません。

## ホットキー

設定から次のホットキーを変更できます。

- Kipple を開くグローバルホットキー
- エディタ内容を履歴に保存するホットキー
- エディタをクリアするホットキー
- 画面テキストキャプチャのホットキー

ホットキーが他のアプリや macOS の設定と競合している場合は、別の組み合わせを選んでください。

## 設定

設定画面は4つのタブに分かれています。

### General

- 表示言語
- ログイン時に起動
- Kipple を開くグローバルホットキー
- ウィンドウアニメーション

### Editor

- エディタ位置
- 保存ホットキー
- クリアホットキー

### Clipboard

- URL を開く修飾クリック
- カテゴリ管理
- Auto Pin
- 最大履歴数
- 最大ピン留め数
- 履歴削除
- 画面テキストキャプチャ
- Paste on Selection

### Permission

- 「画面収録とシステムオーディオ録音」の許可状態の確認
- 「デバイスの制御とデータへのアクセス」の許可状態の確認
- 権限が必要な理由
- macOS の System Settings への案内

## Auto Pin

Auto Pin を有効にすると、同じテキストを短時間に繰り返しコピーしたとき、自動的にピン留めできます。

設定できる項目は次の通りです。

- 有効 / 無効
- 繰り返しコピーとみなす時間
- ピン留めに必要なコピー回数

履歴からの再コピーは Auto Pin の判定に含まれません。
外部アプリで繰り返しコピーした内容が対象です。

## データ管理

履歴の最大件数は設定から変更できます。
初期値は 300 件で、10〜1000 件の範囲で設定できます。

履歴をまとめて削除することもできます。
この操作では、ピン留めされていない履歴だけを削除し、ピン留め項目は残します。

保存対象はテキストです。
画像やファイルそのものを履歴として保存する機能はありません。

## プライバシー

Kipple はローカルで動作するアプリです。

- クリップボード履歴は Mac の中に保存
- 設定は UserDefaults に保存
- カテゴリは UserDefaults に保存
- 履歴は SwiftData に保存
- OCR は端末内で処理
- 分析、トラッキング、クラウド同期は使用しない

クリップボードには機密情報が含まれることがあります。
不要になった履歴は設定画面から削除できます。

## 必要な権限

システム設定 → プライバシーとセキュリティで、以下の項目を開いて Kipple を有効にします。Kippleの設定 → 権限からも対象画面を開けます。

許可後はKippleに戻って状態を確認してください。macOSから再起動を求められた場合や「未許可」のままの場合は、Kippleを終了して起動し直してください。それでも「未許可」の場合は、該当する権限の一覧から古いKippleを削除し、現在使っているKipple.appを追加して許可し直してください。署名や起動するアプリを変更した場合は、以前の許可が適用されないことがあります。

### 画面収録とシステムオーディオ録音

Screen Text Capture で、ユーザーが選択した画面領域を読み取るために必要です。
この権限がない場合、画面テキストキャプチャは使えません。コピー元のウインドウ名を取得できないこともあります。音声は録音しません。

### デバイスの制御とデータへのアクセス

Queue・Paste on Selection・書式なし貼り付けで、貼り付け先のアプリへ Command + V を送信するために必要です。
この権限がない場合、該当機能は無効になります。

書式なし貼り付けのショートカットは、許可後に設定 → General → Pastingで変更・解除できます。許可の取り消し時はショートカットを停止し、設定したキーは保持します。

通常モードでは、書式なしで貼り付けた後も、書式付きのショートカットでKippleが保持する元のコピーを貼り付けられます。次の明示的な貼り付け操作で切り替えるため、時間で書式を戻す処理や貼り付け先アプリごとの対応はありません。新しいコピーが入ると退避データを破棄します。メニューやマウスでの貼り付けは、その時点のクリップボードの内容を使います。

通常の履歴保存・検索・履歴の再コピーには、これらの許可は不要です。入力監視・オートメーションの許可も不要です。MCPはローカルのUnixソケットで通信するため、ローカルネットワークの許可は不要です。

クリップボードの読み取り制御は画面収録・アクセシビリティとは別です。設定 → 権限 → クリップボードへのアクセスで状態と再試行を確認できます。許可変更後は同じクリップボード内容を再取得し、拒否された同じ読み取りを自動で繰り返しません。

終了時は保存完了を待ち、保存失敗やタイムアウトの場合は終了を取り消して案内を表示します。

## 動作環境

- macOS 27.0 以降
- Apple Silicon（M1以降）搭載のMac

## できないこと

現在の Kipple は、テキスト中心のクリップボードマネージャーです。

- 画像履歴の保存
- ファイル履歴の保存
- クラウド同期
- チーム共有
- Web サービスへの履歴送信

これらの機能は現在の実装には含まれていません。
