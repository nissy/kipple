# Kipple の MCP 連携

外部AIから本文を受け取り、Kippleの履歴へ登録して先頭の1件をクリップボードにコピーします。
他のアプリへの自動貼り付けや、履歴・クリップボードの読み取りは行いません。

## 初回設定

1. Kippleの「設定 → AI連携」でMCPを有効にします。
2. 「接続設定をコピー」を押し、AIクライアントのMCP設定に登録します。
3. AIに「この文章をKippleに登録して」と依頼します。

接続設定には同梱ヘルパーの実行パスだけが含まれます。接続トークン・接続ID・接続名の入力はありません。
複数のクライアントに同じ設定を登録できます。MCPをオフにして再びオンにしても設定は変わりません。
アプリの設置場所を変更した場合は、新しいパスの設定をコピーし直してください。

```json
{
  "mcpServers": {
    "kipple": {
      "command": "/Applications/Kipple.app/Contents/Helpers/KippleMCP"
    }
  }
}
```

設定ファイルの形式はAIクライアントに合わせてください。
対応クライアントでは、署名済みヘルパーを含む `.mcpb` も利用できます。認証情報の入力は不要です。

## 登録ツール

`kipple_add_items` を公開します。

```json
{
  "requestId": "dfd42916-d841-4785-98a1-06e49169714a",
  "items": [
    {
      "content": "そのまま登録する本文",
      "title": "返信文案"
    }
  ]
}
```

- `requestId`: UUID。24時間以内の再送では同じIDを使います。
- `items`: 1〜50件。本文 `content` は各項目に必須です。
- `title`: 用途が分かる短いタイトル。任意、最大120文字。

各項目は `content` と `title` のみ受け付けます。その他の項目は `INVALID_INPUT` で拒否します。

本文は **1件80,000文字まで** です。JSON Schemaに合わせUnicodeコードポイント数で数えます。
日本語・半角英数字・単独の絵文字は通常1文字ですが、結合文字や複数コードポイントからなる絵文字はその数だけ数えます。
UTF-8の256KiB制限はありません。例えば単独の絵文字80,000文字（320,000バイト）も受け付けます。

空文字、NUL、80,001文字以上の本文は拒否します。切り捨てや分割登録はしません。
バッチ内に不正な項目があれば全件を拒否し、クリップボードも変更しません。
通信全体の上限はJSON 24MiB、バッチ本文合計16MiBです。
ファイルパス入力と追記ツールは提供しません。AI側から本文を直接送ってください。

全件を履歴へ登録し、先頭の本文をコピーします。履歴の登録元はMCPと表示します。
本文・Title・登録元・初回登録日時を通常の履歴として保存します。
既存の同一本文と統合するときは、ID・ピン留め・カテゴリ・初回登録日時を維持します。
Titleが省略された場合は既存のTitleを引き継ぎます。
履歴の詳細でTitleを変更しても、クリップボードや履歴の順序は変わりません。

## 再送と結果

同じ `requestId`・同じ内容なら、保存済み結果を返します。再登録・再コピーはしません。
同じIDで違う内容を送ると `REQUEST_ID_CONFLICT` を返します。
処理記録は24時間保持します。本文やタイトルは応答に含めません。

履歴保存とmacOSのクリップボード更新は一括確定できません。
保存後にコピーが失敗した場合は `clipboard_failed`、結果を確定できない場合は `clipboard_unknown` を返します。
MCPを無効にすると進行中の未確定処理も止め、書き込み直前にも有効状態を確認します。
既に保存された履歴は取り消しません。

## ローカル通信

stdioヘルパーから同一ユーザー専用のUnix domain socketに通信します。
HTTP/TCPポートは開きません。ソケットの権限は0600とし、接続元ユーザーを確認します。
同じユーザーのローカルプログラムを信頼する設計で、クライアントごとの認証や失効は行いません。
MCPを通してファイルを読ませる機能もありません。

接続トークンは生成・保存しません。Keychainの読み書きもありません。
クリップボードには本文だけを書き込みます。管理マーカーや内部署名鍵は使いません。
再送時の内容照合にはリクエストのSHA-256を使い、処理結果とともにローカルDBへ保存します。
旧接続設定や旧保存形式の移行・互換処理は提供しません。

## 開発と配布

`project.yml` からアプリと同梱ヘルパーを生成します。
Releaseでは、本体とヘルパーに同じApp Groupの署名・権限が必要です。
署名を省くDebugでは、両者が `/private/tmp/kipple-mcp-debug-<ユーザーID>/mcp.sock` を使います。
専用ディレクトリは同じユーザーが所有する0700の実ディレクトリに限定し、シンボリックリンクは拒否します。
テストホストにはさらに `-test-<プロセスID>` を付け、実行中の開発版の通信先から分離します。
ReleaseでApp Groupが使えない場合にDebugの通信先へ切り替える処理はありません。

```sh
make lint
make test
make mcpb MCP_HELPER=build/release/Kipple.app/Contents/Helpers/KippleMCP
```

署名を省く単体テストだけでは配布構成のApp Group到達性を確認できません。
リリース前に署名済みアプリで、実際のAIクライアントから接続・登録を確認してください。
